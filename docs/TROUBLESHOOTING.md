# Troubleshooting

This page collects common issues when running the LBX conversion pipeline.

## Common issues
- Missing module errors: install dependencies with
  `python3 -m pip install -r pip_requirements.txt`.
- Output is empty: confirm the input path points at a folder containing
  `.lbx` files and re-run.

## Known gaps
- Add troubleshooting steps for print calibration.
